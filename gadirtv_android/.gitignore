# Flutter/Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
.fvm/
.metadata

# IDE
.idea/
.vscode/
*.iml
*.iws

# Android
android/.gradle/
android/local.properties
android/**/gradle-wrapper.jar
android/**/gradlew
android/**/gradlew.bat
android/app/release/
# NOTE: key.properties and gadirtv-release.jks ARE committed intentionally so
# every CI build signs with the same certificate (allows OTA updates on device
# without "package conflict" errors). Only real production keys should be kept
# out of the repo.
# android/key.properties
*.keystore

# iOS (not used but ignored)
ios/

# OS
.DS_Store
Thumbs.db
